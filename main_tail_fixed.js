async function pdfToWord() {  const file = state.pdf2word.file;  if (!file) { showToast('Please select a PDF file.', 'error'); return; }  showResult('pdf2word', '');  setProgress('pdf2word', 10, 'Loading PDF…');  setButtonEnabled('btn-pdf2word', false);  try {    const arrayBuffer = await file.arrayBuffer();    setProgress('pdf2word', 50, 'Converting to Word format...');    const blob = await window.convertPDFToWordIsolated(arrayBuffer);    const name = file.name.replace(/\.pdf$/i, '') + '.docx'; /* proper Word format */    setProgress('pdf2word', 100, 'Complete!');    showResult('pdf2word', successResult(name, blob, formatSize(blob.size)));    showToast('PDF converted to Word successfully!');  } catch (err) {    setProgress('pdf2word', null);    showResult('pdf2word', errorResult('Conversion failed: ' + err.message));    showToast('Conversion failed: ' + err.message, 'error');  } finally {    setButtonEnabled('btn-pdf2word', !!state.pdf2word.file);    setTimeout(() => setProgress('pdf2word', null), 1500);  }}/* ──────────────────────────────────────────────────   12. WORD TO PDF   Uses mammoth.js to extract HTML from .docx,   then html2canvas + pdf-lib to render to PDF.   Preserves text, images, tables, and formatting.   ────────────────────────────────────────────────── */async function wordToPDF() {  const file = state.word2pdf.file;  if (!file) { showToast('Please select a Word document.', 'error'); return; }  if (typeof mammoth === 'undefined') {    showToast('mammoth.js library not loaded. Please check your internet connection.', 'error');    return;  }  if (typeof html2canvas === 'undefined') {    showToast('html2canvas library not loaded. Please check your internet connection.', 'error');    return;  }  showResult('word2pdf', '');  setProgress('word2pdf', 5, 'Reading Word document…');  setButtonEnabled('btn-word2pdf', false);  try {    const arrayBuffer = await file.arrayBuffer();    setProgress('word2pdf', 20, 'Extracting content…');    const result = await mammoth.convertToHtml({      arrayBuffer,      convertImage: mammoth.images.imgElement(image => {        return image.read('base64').then(imageContents => ({          src: `data:${image.contentType};base64,${imageContents}`        }));      }),    });    const htmlContent = result.value;    setProgress('word2pdf', 40, 'Rendering document…');    /* Create an off-screen container styled like a Word page */    const container = document.createElement('div');    container.id = 'w2p-render-container';    container.style.cssText = [      'position:fixed',      'left:-9999px',      'top:0',      'width:794px',      'min-height:1123px',      'padding:72px 90px',      'background:#ffffff',      'color:#1a1a1a',      /* Don't hardcode a single font — let CSS handle per-script font selection */      'font-size:12pt',      'line-height:1.6',      'box-sizing:border-box',      'word-break:break-word',      'overflow:hidden',      /* CRITICAL: let browser BiDi engine handle directional layout */      'unicode-bidi:plaintext',    ].join(';');    /* ── Post-process mammoth HTML for proper Arabic/RTL rendering ────────── */    /* mammoth.js strips dir= and lang= from the .docx content. We must restore */    /* them so the browser's Unicode Bidirectional Algorithm can shape Arabic */    /* glyphs correctly *before* html2canvas photographs the DOM. */    const ARABIC_REGEX = /[\u0600-\u06FF\u0750-\u077F\u08A0-\u08FF\uFB50-\uFDFD\uFE70-\uFEFF]/;    /**     * Walk every block-level element in a DocumentFragment.     * If its text content contains Arabic characters, mark it RTL so the     * browser's built-in BiDi engine handles shaping and justification.     */    function applyBidiToFragment(frag) {      /* Block elements that carry a reading direction in Word */      const BLOCKS = ['P', 'H1', 'H2', 'H3', 'H4', 'H5', 'H6',        'LI', 'TD', 'TH', 'BLOCKQUOTE', 'DIV'];      frag.querySelectorAll(BLOCKS.join(',')).forEach(el => {        const text = el.textContent || '';        if (ARABIC_REGEX.test(text)) {          /* Use bdi on inline runs; set dir on block so the browser BiDi */          /* algorithm can order runs correctly within the paragraph. */          el.setAttribute('dir', 'rtl');          el.setAttribute('lang', 'ar');          /* Wrap inline text nodes in <bdi dir="rtl"> so mixed LTR/RTL lines */          /* (e.g. Arabic paragraph with embedded English code) render cleanly. */          el.childNodes.forEach(node => {            if (node.nodeType === Node.TEXT_NODE && node.textContent.trim()) {              const bdi = document.createElement('bdi');              bdi.setAttribute('dir', ARABIC_REGEX.test(node.textContent) ? 'rtl' : 'ltr');              bdi.style.fontFamily = ARABIC_REGEX.test(node.textContent)                ? "'Arial','Segoe UI','Tahoma',sans-serif"                : "'Times New Roman',Times,serif";              bdi.textContent = node.textContent;              node.replaceWith(bdi);            } else if (node.nodeType === Node.ELEMENT_NODE) {              const inner = node.textContent || '';              if (ARABIC_REGEX.test(inner)) {                node.style.fontFamily = "'Arial','Segoe UI','Tahoma',sans-serif";                node.setAttribute('dir', 'rtl');              }            }          });        } else {          /* Purely LTR block — be explicit so adjacent RTL blocks don't bleed */          if (!el.getAttribute('dir')) {            el.setAttribute('dir', 'ltr');          }        }      });    }    /* Parse mammoth HTML into a live fragment so we can walk the DOM */    const templateFrag = document.createElement('template');    templateFrag.innerHTML = htmlContent || '<p>(Empty document)</p>';    applyBidiToFragment(templateFrag.content);    /* Serialize fragment back to HTML string for injection */    const rtlAwareHtml = Array.from(templateFrag.content.childNodes)      .map(n => (n.nodeType === Node.ELEMENT_NODE ? n.outerHTML : n.textContent))      .join('');    /* Add inline styles for common HTML elements */    /* Note: Arabic text gets Arial/Segoe UI via per-element injection above. */    /* LTR text keeps Times New Roman. This mirrors Microsoft Word defaults. */    container.innerHTML = `      <style>        #w2p-render-container {          font-family: "Times New Roman", Times, serif;          unicode-bidi: plaintext;        }        #w2p-render-container h1{font-size:22pt;margin:16px 0 8px;line-height:1.3;}        #w2p-render-container h2{font-size:18pt;margin:14px 0 6px;}        #w2p-render-container h3{font-size:14pt;margin:12px 0 4px;}        #w2p-render-container p{margin:0 0 8px;unicode-bidi:plaintext;}        #w2p-render-container table{border-collapse:collapse;width:100%;margin-bottom:12px;}        #w2p-render-container td,#w2p-render-container th{border:1px solid #ccc;padding:6px 8px;unicode-bidi:plaintext;}        #w2p-render-container img{max-width:100%;height:auto;display:block;margin:8px auto;}        #w2p-render-container ul,#w2p-render-container ol{margin:0 0 8px 24px;}        #w2p-render-container li{margin-bottom:4px;unicode-bidi:plaintext;}        #w2p-render-container strong{font-weight:bold;}        #w2p-render-container em{font-style:italic;}        #w2p-render-container blockquote{border-left:3px solid #ccc;margin:8px 0;padding-left:16px;color:#555;}        /* Arabic blocks — right-aligned, correct font */        #w2p-render-container [dir="rtl"]{          text-align:right;          font-family:'Arial','Segoe UI','Tahoma',sans-serif;        }        /* Mixed paragraph: isolate each run's direction */        #w2p-render-container bdi{unicode-bidi:isolate;}      </style>      ${rtlAwareHtml}    `;    document.body.appendChild(container);    /* Wait for images to load */    const imgs = container.querySelectorAll('img');    await Promise.all(Array.from(imgs).map(img =>      img.complete ? Promise.resolve() : new Promise(r => { img.onload = r; img.onerror = r; })    ));    setProgress('word2pdf', 55, 'Rendering to canvas…');    const fullCanvas = await html2canvas(container, {      scale: 2,      useCORS: true,      allowTaint: true,      backgroundColor: '#ffffff',      logging: false,      width: 794,      windowWidth: 794,    });    document.body.removeChild(container);    /* A4 in PDF points */    const A4W = 595, A4H = 842;    const imgScale = A4W / (fullCanvas.width / 2); /* /2 because scale:2 */    const scaledHeight = (fullCanvas.height / 2) * imgScale;    const numPdfPages = Math.max(1, Math.ceil(scaledHeight / A4H));    setProgress('word2pdf', 72, `Building PDF (${numPdfPages} page${numPdfPages !== 1 ? 's' : ''})…`);    const pdfDoc = await PDFLib.PDFDocument.create();    pdfDoc.setTitle(file.name.replace(/\.(docx?)$/i, ''));    pdfDoc.setCreator('PDF BOX');    pdfDoc.setProducer('PDF BOX — pdfbox.app');    for (let p = 0; p < numPdfPages; p++) {      const srcY = p * (A4H / imgScale) * 2; /* *2 for canvas scale */      const srcH = Math.min((A4H / imgScale) * 2, fullCanvas.height - srcY);      if (srcH <= 0) break;      const pageCanvas = document.createElement('canvas');      pageCanvas.width = fullCanvas.width;      pageCanvas.height = Math.ceil(srcH);      const pCtx = pageCanvas.getContext('2d');      pCtx.fillStyle = '#ffffff';      pCtx.fillRect(0, 0, pageCanvas.width, pageCanvas.height);      pCtx.drawImage(fullCanvas, 0, -srcY);      const jpegDataUrl = pageCanvas.toDataURL('image/jpeg', 0.94);      const jpegBlob = dataURLtoBlob(jpegDataUrl);      const jpegBuf = await jpegBlob.arrayBuffer();      const embImg = await pdfDoc.embedJpg(new Uint8Array(jpegBuf));      const page = pdfDoc.addPage([A4W, A4H]);      const drawH = Math.min(A4H, (srcH / 2) * imgScale);      page.drawImage(embImg, { x: 0, y: A4H - drawH, width: A4W, height: drawH });      setProgress('word2pdf', 72 + Math.round((p / numPdfPages) * 20), `Rendering page ${p + 1}…`);    }    setProgress('word2pdf', 95, 'Saving PDF…');    const out = await pdfDoc.save();    const blob = new Blob([out], { type: 'application/pdf' });    const name = file.name.replace(/\.(docx?)$/i, '') + '.pdf';    setProgress('word2pdf', 100, 'Complete!');    showResult('word2pdf', successResult(name, blob, `${numPdfPages} page${numPdfPages !== 1 ? 's' : ''} · ${formatSize(blob.size)}`));    showToast('Word document converted to PDF successfully!');  } catch (err) {    const container2 = document.getElementById('w2p-render-container');    if (container2) container2.remove();    setProgress('word2pdf', null);    showResult('word2pdf', errorResult('Conversion failed: ' + err.message));    showToast('Conversion failed: ' + err.message, 'error');  } finally {    setButtonEnabled('btn-word2pdf', !!state.word2pdf.file);    setTimeout(() => setProgress('word2pdf', null), 1500);  }}/* ──────────────────────────────────────────────────   CONTACT FORM   ────────────────────────────────────────────────── */function sendContact() {  const name = (document.getElementById('contact-name').value || '').trim();  const email = (document.getElementById('contact-email').value || '').trim();  const subject = (document.getElementById('contact-subject').value || '').trim();  const message = (document.getElementById('contact-message').value || '').trim();  if (!name || !email || !message) {    showToast('Please fill in all required fields.', 'error');    return;  }  if (!/\S+@\S+\.\S+/.test(email)) {    showToast('Please enter a valid email address.', 'error');    return;  }  const subjectLine = encodeURIComponent(`PDF BOX Contact${subject ? ' – ' + subject : ''} (from ${name})`);  const body = encodeURIComponent(    `Name: ${name}\nEmail: ${email}\n\nMessage:\n${message}`  );  window.open(`https://mail.google.com/mail/?view=cm&fs=1&to=hasan.saad898@gmail.com&su=${subjectLine}&body=${body}`, '_blank');  showToast('Opening Gmail…');  /* Clear form */  document.getElementById('contact-name').value = '';  document.getElementById('contact-email').value = '';  document.getElementById('contact-subject').value = '';  document.getElementById('contact-message').value = '';}/* ──────────────────────────────────────────────────   FAQ ACCORDION   ────────────────────────────────────────────────── */function toggleFAQ(btn) {  const item = btn.parentElement;  const isOpen = item.classList.contains('open');  document.querySelectorAll('.faq-item').forEach(i => i.classList.remove('open'));  if (!isOpen) item.classList.add('open');}/* ──────────────────────────────────────────────────   CARD GLOW (mouse tracking)   ────────────────────────────────────────────────── */document.querySelectorAll('.tool-card').forEach(card => {  card.addEventListener('mousemove', (e) => {    const rect = card.getBoundingClientRect();    const mx = ((e.clientX - rect.left) / rect.width) * 100;    const my = ((e.clientY - rect.top) / rect.height) * 100;    card.style.setProperty('--mx', mx + '%');    card.style.setProperty('--my', my + '%');  });});/* ──────────────────────────────────────────────────   INTERSECTION OBSERVER (animate on scroll)   ────────────────────────────────────────────────── */const observerOpts = { threshold: 0.12 };const observer = new IntersectionObserver((entries) => {  entries.forEach(entry => {    if (entry.isIntersecting) {      entry.target.style.opacity = '1';      entry.target.style.transform = 'translateY(0)';      observer.unobserve(entry.target);    }  });}, observerOpts);document.querySelectorAll('.tool-card, .feature-card, .faq-item').forEach((el, i) => {  el.style.opacity = '0';  el.style.transform = 'translateY(30px)';  el.style.transition = `opacity 0.5s ease ${i * 0.05}s, transform 0.5s ease ${i * 0.05}s`;  observer.observe(el);});/* ──────────────────────────────────────────────────   SMOOTH SCROLL for anchor links   ────────────────────────────────────────────────── */document.querySelectorAll('a[href^="#"]').forEach(link => {  link.addEventListener('click', (e) => {    const id = link.getAttribute('href').slice(1);    const target = document.getElementById(id);    if (target) {      e.preventDefault();      target.scrollIntoView({ behavior: 'smooth', block: 'start' });    }    closeMobileMenu();  });});/* ──────────────────────────────────────────────────   INIT   ────────────────────────────────────────────────── */console.log('%c PDF BOX 📦 Ready! — 12 Tools Active', 'color:#00E5FF;font-size:16px;font-weight:bold;');